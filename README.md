# Research-digest-agent
An autonomous research digest agent that extracts key claims from multiple sources (URLs/files), removes redundancy using semantic similarity, preserves conflicting viewpoints, and generates structured evidence-backed research briefs.
